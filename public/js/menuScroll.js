// Добавление действий при прокрутке страницыtry {    window.addEventListener('scroll', menuScroll, false); // Добавление события для W3C} catch (e) {    window.attachEvent('onscroll', menuScroll); // Добавление события для IE}// Добавление функционала постоянного нахождения меню на экранеfunction menuScroll() {    var menu = document.getElementById('menu');    if (menu.style.position != 'absolute') {        if ((document.documentElement.scrollTop || document.body.scrollTop) >= menu.offsetTop) {			// Добавление заглушки на место меню            var footprint = document.createElement('div');            footprint.setAttribute('id', 'footprint');            footprint.style.height = menu.offsetHeight + 'px';            var content = document.getElementById('content');            menu.parentNode.insertBefore(footprint, content);            // Фиксация меню вверху экрана            menu.style.position = 'absolute';            menu.style.top = (document.documentElement.scrollTop || document.body.scrollTop) + 'px';            menu.style.left = '0px';            menu.style.backgroundColor = '#ffffff';            menu.style.opacity = '0.8';            menu.style.width = '99%';        }    }    else {        menu.style.top = (document.documentElement.scrollTop || document.body.scrollTop) + 'px';        var footprint = document.getElementById('footprint');        if ((document.documentElement.scrollTop || document.body.scrollTop) <= footprint.offsetTop) {            // Возврат меню на прежнее место в потоке            menu.style.position = '';            menu.style.top = '';            menu.style.left = '';            menu.style.backgroundColor = '';            menu.style.opacity = '';            menu.style.width = '';            // Удаление заглушки с места меню            footprint.parentNode.removeChild(footprint);        }    }}// Добавление действий при прокрутке страницыtry {    window.addEventListener('scroll', menuScroll2, false); // Добавление события для W3C} catch (e) {    window.attachEvent('onscroll', menuScroll2); // Добавление события для IE}// Добавление функционала постоянного нахождения меню на экранеfunction menuScroll2() {    var menu = document.getElementsByTagName('nav')[0];    if (menu.style.position != 'absolute') {        if ((document.documentElement.scrollTop || document.body.scrollTop) >= menu.offsetTop) {			// Добавление заглушки на место меню            var footprint = document.createElement('div');            footprint.setAttribute('id', 'footprint');            footprint.style.height = menu.offsetHeight + 'px';            var content = document.getElementById('content');            menu.parentNode.insertBefore(footprint, content);            // Фиксация меню вверху экрана            menu.style.position = 'absolute';            menu.style.top = (document.documentElement.scrollTop || document.body.scrollTop) + 'px';            menu.style.left = '0px';            menu.style.backgroundColor = '#ffffff';            menu.style.opacity = '0.8';            menu.style.width = '99%';        }    }    else {        menu.style.top = (document.documentElement.scrollTop || document.body.scrollTop) + 'px';        var footprint = document.getElementById('footprint');        if ((document.documentElement.scrollTop || document.body.scrollTop) <= footprint.offsetTop) {            // Возврат меню на прежнее место в потоке            menu.style.position = '';            menu.style.top = '';            menu.style.left = '';            menu.style.backgroundColor = '';            menu.style.opacity = '';            menu.style.width = '';            // Удаление заглушки с места меню            footprint.parentNode.removeChild(footprint);        }    }}